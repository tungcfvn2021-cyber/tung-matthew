# ============================================================
#  TÙNG MATTHEW BARBER — Environment variables
#  Sao chép file này thành `.env` và điền giá trị thật.
#  KHÔNG commit file `.env` (đã nằm trong .gitignore).
# ============================================================

# --- Bắt buộc (Sprint 1) ---
# PostgreSQL — dùng Neon hoặc Supabase (serverless, có SSL)
DATABASE_URL="postgresql://user:password@host:5432/tung_matthew?sslmode=require"

# URL gốc của site (dùng cho metadata, OpenGraph, canonical)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# --- Auth (Sprint 4 — Better Auth) ---
BETTER_AUTH_SECRET=""
BETTER_AUTH_URL="http://localhost:3000"

# --- Cloudinary (Sprint 5 — ảnh Lookbook/dịch vụ) ---
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""

# --- Zalo OA / ZNS (Sprint 3 — xác nhận & nhắc lịch) ---
ZALO_OA_ID=""
ZALO_OA_SECRET=""
ZALO_ACCESS_TOKEN=""

# --- Resend (Sprint 3 — email thông báo admin) ---
RESEND_API_KEY=""
ADMIN_NOTIFY_EMAIL=""

# --- Google (Sprint 4/5 — Maps + đồng bộ review) ---
GOOGLE_MAPS_API_KEY=""
GOOGLE_PLACES_API_KEY=""

# --- Analytics / Pixel (Sprint 5) ---
NEXT_PUBLIC_GA4_ID=""
NEXT_PUBLIC_META_PIXEL_ID=""
NEXT_PUBLIC_TIKTOK_PIXEL_ID=""

# --- Cron (Sprint 5 — nhắc lịch Phase 2) ---
CRON_SECRET=""
